spring:
  boot:
    admin:
      client:
        url: http://localhost:9001
        instance:
          metadata:
            user.name: admin-client-user
            user.password: admin-client-password

logging:
  level:
    org.filio: DEBUG
