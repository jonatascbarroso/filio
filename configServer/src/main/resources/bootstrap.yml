server:
  port: 9002

spring:
  application:
    name: configServer
  profiles:
    active: native
  cloud:
    config:
      server:
        bootstrap: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
