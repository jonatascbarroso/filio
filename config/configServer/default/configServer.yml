spring:
  security:
    user:
      name: instance-user2
      password: instance-password2
  boot:
    admin:
      client:
        username: admin-server-user2
        password: admin-server-password2
        instance:
          metadata:
            user.name: instance-user2
            user.password: instance-password2

logging:
  level:
    org.filio: DEBUG
