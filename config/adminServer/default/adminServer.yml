spring:
  security:
    user:
      name: admin-server-user2
      password: admin-server-password2
  boot:
    admin:
      client:
        username: instance-user
        password: instance-password

logging:
  level:
    org.filio: DEBUG
